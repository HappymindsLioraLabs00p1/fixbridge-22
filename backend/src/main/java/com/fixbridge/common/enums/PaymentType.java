package com.fixbridge.common.enums;

/** Matches PostgreSQL {@code payment_type}. */
public enum PaymentType {
    dispatch_fee, managed_repair, deposit, progress, final_payment, lead_fee, subscription
}
