package com.fixbridge.common.enums;

/** Matches PostgreSQL {@code contractor_status}. */
public enum ContractorStatus {
    draft, documents_pending, under_review, approved, suspended, expired, rejected
}
